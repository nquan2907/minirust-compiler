# minirust-compiler
A compiler frontend and type checker for MiniRust, a statically typed procedural language featuring immutable-by-default semantics, custom structs, bottom-up type inference, and control flow validation.
